#ifndef CALCULATOR_H    // CALCULATOR_H 매크로가 정의되어 있지 않은 경우에만 아래 내용을 처리합니다.
#define CALCULATOR_H    // CALCULATOR_H 매크로를 정의합니다.


class Calculator        //계산기 클래스 선언
{
public:
    void run();         // run() 멤버 함수를 선언합니다. 이 함수는 계산기의 작동을 시작합니다.
};

#endif                  // CALCULATOR_H 매크로를 닫습니다.